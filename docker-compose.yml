version: "3.3"
services:
  ciftools:
    image: ghcr.io/cheminfo-py/dimensionality_reduction:latest
    restart: always
    environment:
      - PORT=${PORT}
    ports:
      - 127.0.0.1:${PORT}:${PORT}
